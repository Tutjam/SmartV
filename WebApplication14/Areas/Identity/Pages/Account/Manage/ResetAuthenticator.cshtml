@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reset authenticator key";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Jeśli zresetujesz klucz uwierzytelniający, aplikacja uwierzytelniająca nie będzie działać, dopóki go nie ponownie skonfigurujesz.</strong>
</p>
    <p>
        Ten proces wyłącza logowanie dwuetapowe do momentu zweryfikowania aplikacji uwierzytelniającej.
        Jeśli nie dokonasz konfiguracji aplikacji uwierzytelniającej, możesz utracić dostęp do swojego konta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post" class="form-group">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Zrestartuj klucz uwierzytelniania dwuetapowego.</button>
    </form>
</div>